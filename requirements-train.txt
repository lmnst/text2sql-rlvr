# Training environment: Linux + NVIDIA GPU only. Do not install locally.
#
# STATUS: NOT YET PINNED. Nothing in this file has been installed or run.
# Leaving it unpinned is the known risk documented in AGENTS.md, not an
# oversight -- verl's trainer interfaces move fast enough that a version chosen
# today without running it would be a guess dressed up as a lockfile.
#
# Fill it in like this, once, on the GPU box:
#
#   1. Install torch matching the box's CUDA, then vllm, then verl from source.
#   2. Run verl's official gsm8k GRPO example end to end. Do not skip this --
#      it separates "my reward function is wrong" from "my environment is wrong",
#      and those two failures look identical from the logs.
#   3. Only then: pip freeze > requirements-train.lock.txt, and record the verl
#      commit sha and the lock date below.
#
# verl:         v0.8.0  (released 2026-06-01)
# GPU:          single RTX 4090 24GB, AutoDL
# CUDA / driver: <fill in>
#
# History, because it is the point of this file:
#
# 2026-08-08  Cloned verl at HEAD (4a2cba76f7f605d2b9f56e640faaeaa71c2c7f71)
#             because the runbook said `git clone`, not `git checkout <tag>`.
#             Config validation passed and Ray started, then the run died on
#             `ModuleNotFoundError: No module named 'transfer_queue'` -- HEAD
#             carried a half-landed refactor whose task runner imports a data
#             component that is not a dependency yet.
#
#             AGENTS.md has said "pin verl to a specific commit" since day one.
#             The instruction was written and then not followed. Moving to a
#             released tag.
#
# The rest of this file is still unpinned. Run `pip freeze > requirements-train.lock.txt`
# once a GRPO run has completed end to end, and record the CUDA version here.

torch
vllm
verl
transformers
accelerate
peft
datasets
flash-attn
wandb
